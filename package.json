{
	"name": "backend",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"engines": {
		"node": ">=18.0.0"
	},
	"scripts": {
		"dev": "cross-env NODE_ENV=development node --watch src/server.js",
		"prod": "cross-env NODE_ENV=production node --watch src/server.js",
		"test": "cross-env NODE_ENV=test node --watch src/server.js",
		"pm2prod": "cross-env NODE_ENV=development node --watch src/server.js",
		"vitest": "npx vitest run",
		"test:watch": "npx vitest",
		"test:coverage": "npx vitest run --coverage",
		"test:ui": "npx vitest --ui"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"type": "module",
	"devDependencies": {
		"@biomejs/biome": "1.9.4",
		"@vitest/coverage-v8": "^3.1.2",
		"@vitest/ui": "^3.1.2",
		"cross-env": "^7.0.3",
		"pino-pretty": "^13.0.0",
		"supertest": "^7.1.0",
		"vitest": "^3.1.1"
	},
	"dependencies": {
		"bcryptjs": "^3.0.2",
		"dotenv": "^16.5.0",
		"fastify": "^5.3.2",
		"knex": "^3.1.0",
		"mysql2": "^3.14.0",
		"zod": "^3.24.3"
	}
}
